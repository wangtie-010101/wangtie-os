# wangtie-os 插件 · 上传发布说明

插件名：**wangtie-os**（`package.json` 的 `name` 已一致）
版本：见 `package.json`（当前 0.1.0-rc.1）

## 本包内容
完整可发布的插件源码与构建产物（不含 node_modules）：
- src/（服务端 TS：路由 / 静态托管 / 配置 / 数据 / 数据库连接探测）
- ui/（前端 SPA：9 个功能模块、游戏、内置知识数据）
- client/ 与 lib/（已构建产物）
- package.json / cordis.patch.yml / build.mjs / README.md

## 第一步：上传到你的 GitHub（仓库名请用 wangtie-os）
```sh
cd wangtie-os-upload
git init
git add -A
git commit -m "feat: 王铁 OS 插件 wangtie-os（个人工作台）"
git branch -M main
git remote add origin https://github.com/<你的用户名>/wangtie-os.git
git push -u origin main
```

## 第二步：提交到 DeepSeek Harness 插件专区收录
官方插件市场 dsh-market 的清单来自精选目录：
https://github.com/awesome-dsh-plugin/awesome-dsh-plugin
打开后按该仓库的贡献说明提一个 PR，在插件清单中新增一行：
```
wangtie-os  https://github.com/<你的用户名>/wangtie-os
```
（或按其 README 规定的条目格式填写名称与仓库地址；收录后 dshmarket / dshmarket.com 通常一天内自动显示）

## 本地使用（未发布前的安装方式）
```sh
cd wangtie-os-upload
npm install          # 安装 esbuild 等开发依赖
node build.mjs       # 产出 lib/index.js（本包已含构建产物，可跳过）
cd <dsh 检出目录>
pnpm dsh plugin --profile web add file:<本包绝对路径>
```
重启 `dsh web` 后访问 http://127.0.0.1:3080/wangtie-os
